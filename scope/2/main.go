package main

import "fmt"

func main(){
	a:=5
	fmt.Println(a)
	fmt.Println(b)
}
var b =  6//External Scope can be able to use in internal scope. But, Internal scope can't be use in the external scope.